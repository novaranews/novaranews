<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * NovaNews intentionally creates no default administrator or password.
     * Run `php artisan novara:create-admin` after migrating a new installation.
     */
    public function run(): void
    {
        // Intentionally empty.
    }
}

